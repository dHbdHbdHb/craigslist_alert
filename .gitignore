# ── Secrets — never commit ────────────────────────────────────────────────────
secrets.env

# Generated artifacts (recreated on demand, not for deployment)
email_map.html
email_map.png
analysis_dashboard.png

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Logs
logs/

# Runtime state (per-profile)
data/*/last_digest_date.txt
data/*/bike_routes.json
data/*/bart_bike_routes.json
data/*/transit_commutes.json
# Routes API call ledger. Per-machine like the cache it guards — the Pi and a
# laptop each have their own cache, so they each spend and count separately.
data/*/transit_commutes_budget.json
data/*/digest_preview.html

# Editor backup, written whenever shapes are saved from the map GUI
neighborhoods/neighborhood_shapes.py.bak

# Runtime state (legacy single-profile layout, pre-migration)
last_digest_date.txt
craigslist_data/bike_routes.json
craigslist_data/digest_preview.html
craigslist_data/bart_bike_routes.json

CLAUDE.md
